<%= title @user.disp_name, :h2 %>

<% if current_user = @user %>
<% form_for @user do |f| %>
  <%= f.error_messages %>
  <%= f.text_field :name %>
  <%= submit_tag 'save name' %>
<% end %>
<% end %>

<%= @user.identity_url %>

<h3>wrote</h3>
<%= render :partial => "patches/list",
   :locals =>
     { :patches => @patches,
       :nothing => '(nothing yet)',
       :postonly => true, :pagenum => :page
      } %>

<p>(newest first)</p>
