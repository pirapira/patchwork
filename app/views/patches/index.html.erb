<%= title "Newest", :h2 %> 

<%= render :partial => "largelist",
   :locals =>
     { :patches => @patches,
       :nothing => '(nothing yet)',
       :post => true, :pagenum => :page
      } %>

<p>(newest first) <%= link_to 'rss', :action => :feed -%></p>
