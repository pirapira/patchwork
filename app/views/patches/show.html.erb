<% title @patch.summary %>

<div style="margin-top: 2em">
  
<%= render :partial => "list",
   :locals =>
     { :patches => @prepatches,
       :nothing => '(nothing above)',
       :pagenum => :prepage
 } %>

<p style="margin-bottom: 0; padding-bottom: 0">
<%= link_to 'ADD ABOVE',
    {:action => :new, :postpatch_id => @patch.id},
    {:class => 'action'}
-%></p>

<div style="float: left; width: 30em">
<%= render :partial => 'centralbox',
	   :locals => { :patch => @patch } %>
<%= link_to 'ADD BELOW',
    {:action => :new, :prepatch_id => @patch.id},
    {:class => 'action'}
 -%>
<%= render :partial => "list",
   :locals =>
     { :patches => @postpatches,
       :nothing => '(nothing below)',
       :pagenum => :postpage,
       :postonly => true
 } %>
</div>

<p><%= link_to 'FORK', edit_patch_path, {:class => 'action'} %></p>

<%= render :partial => "list",
   :locals =>
     { :patches => @forks,
       :nothing => '(no forks)',
       :pagenum => :forkpage
 } %>

</div>
