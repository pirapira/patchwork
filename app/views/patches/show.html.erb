<% title @patch.summary %>

<%= render :partial => "list",
   :locals =>
     { :patches => @patch.before_patches,
       :nothing => '(nothing before)' } %>

<p style="margin-bottom: 0.5em">
<%= link_to 'add beforeward',
    {:action => :new, :postpatch_id => @patch.id},
    {:class => 'action'}
-%></p>

<%= render :partial => 'centralbox',
	   :locals => { :float => true, :patch => @patch } %>
<p><%= link_to 'fork', edit_patch_path, {:class => 'action'} %></p>

<%= render :partial => "list",
   :locals =>
     { :patches => @patch.forks,
       :nothing => '(no forks)' } %>

<p style="clear: left; padding-top: 0.5em;">
<%= link_to 'add afterward',
    {:action => :new, :prepatch_id => @patch.id},
    {:class => 'action'}
 -%></p>


<%= render :partial => "list",
   :locals =>
     { :patches => @patch.after_patches,
       :nothing => '(nothing after)' } %>



